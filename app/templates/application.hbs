<table class="table">
  <thead>
    <tr>
      <th>AlbumId</th>
      <th>Title</th>
      <th>Image</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |item|}}
      <tr>
        <td scope="row">{{item.albumId}}</td>
        <td>{{item.title}}</td>
        <td>
          <img src={{item.thumbnailUrl}} alt="Logo">
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
